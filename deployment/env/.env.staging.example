# ============================================
# Staging Environment Variables
# Скопируйте этот файл на сервер в /etc/compauto-staging/.env
# ============================================

# NextAuth Configuration
# Можно использовать тот же секрет что и в production, или отдельный
NEXTAUTH_SECRET=your-staging-secret-here-generate-with-openssl-rand
NEXTAUTH_URL=https://killiskadev.ru

# Database Configuration (Staging)
# ВАЖНО: Используйте ОТДЕЛЬНУЮ базу данных для staging!
# Формат: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=app
DATABASE_URL="postgresql://compauto_user:YOUR_STRONG_PASSWORD@localhost:5432/compauto_staging?schema=app"

# Shadow Database (для безопасных миграций Prisma)
# Prisma использует shadow database для проверки миграций перед применением к основной базе
SHADOW_DATABASE_URL="postgresql://compauto_user:YOUR_STRONG_PASSWORD@localhost:5432/compauto_staging_shadow?schema=app"

# Admin User Credentials (те же что и в production, для удобства тестирования)
ADMIN_EMAIL=admin@komp-auto.ru
ADMIN_PASSWORD=YOUR_STRONG_ADMIN_PASSWORD

# Developer Access (опционально)
DEVELOPER_EMAIL=kirillos382000@mail.ru
DEVELOPER_PASSWORD=YOUR_DEVELOPER_PASSWORD

# Cron Secret (можно использовать тот же или отдельный)
CRON_SECRET=your-cron-secret-key-generate-with-openssl-rand

# Node Environment
NODE_ENV=production
